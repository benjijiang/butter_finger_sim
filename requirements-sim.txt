# Mac simulation dependencies. Install inside the Mac's own .venv only:
#     python -m pip install -r requirements-sim.txt
# Do NOT install these on the Windows authoring machine.
pybullet>=3.2.5,<4
numpy>=1.24,<3
PyYAML>=6.0,<7
pytest>=7.4
