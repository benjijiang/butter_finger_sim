# Simulation dependencies (Linux sim machine primary, Mac fallback).
# Install inside the sim machine's own .venv only:
#     python -m pip install -r requirements-sim.txt
# Do NOT install these on the Windows authoring machine.
pybullet>=3.2.5,<4
numpy>=1.24,<3
PyYAML>=6.0,<7
Pillow>=12.3,<13
opencv-python>=4.8,<5
pytest>=7.4
